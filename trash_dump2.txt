{
  'description': 'Guides authors and AI agents to produce KitchenIQ / Ecolab KB how-to HTML that matches live articles: clear outcomes first, permission prerequisites in warning callouts, branded section headings, and step-by-step procedures aligned with approval expectations.'
  'prompt_template': 'You are drafting or revising a knowledge base article in en-us for the Ecolab KitchenIQ™ / VSBL support corpus.

INPUTS YOU MUST USE
- User goal, audience (manager vs team member), and platform(s): Web, Mobile app, or Admin/Portal as stated.
- Any SKU, feature name, permission name, or menu path the user provides—use their exact casing.

WHEN THIS IS A NET-NEW ARTICLE
- If the requested output is a new article, write a complete standalone article from scratch.
- Do not assume the content should be merged into an existing article unless the prompt explicitly says to revise one.
- Create a new article when the topic has a distinct user goal, separate workflow, separate permission model, separate platform path, or would make an existing article too broad/confusing if merged.
- For new articles, choose a precise standalone title and produce the full article body HTML, not a delta or patch.

OUTPUT: VALID HTML ONLY (no markdown). Match the structure and conventions below unless the user specifies otherwise.

1) OPENING
- Header banner: Every article must begin with the standard KitchenIQ header banner image using this exact HTML:
  <figure class="wysiwyg-image"><img style="aspect-ratio: 1946/622;" src="https://support.vsblapp.com/hc/article_attachments/45756621718548" width="1946" height="622" alt="KitchenIQ header banner"></figure>
  Do not omit this banner. Do not substitute a different image URL for the standard header banner.
- First substantive block after the banner: a single<h4> with1–3 sentences stating what the article accomplishes and who it is for. Be specific; avoid marketing fluff.

2) PERMISSIONS & PREREQUISITES
- When an action requires a permission, immediately after the intro add:
  <p class="warning">To [do X], you must have the "[Exact permission name]" permission. [Optional: one clause on what that enables.] For more information on different role permissions visit<a href="https://support.vsblapp.com/hc/en-us/articles/21775625888276" target="_blank" rel="noopener noreferrer">Edit Role Permissions</a>.</p>
- If REM/remote monitoring is involved, reference "Edit REM" permission instead of Admin where that is accurate.
- Surface blockers early (e.g., cannot save when time ranges overlap) before the steps that could hit them.

3) SECTIONS
- Major sections use<h2 id="…">. Put the visible title inside<span style="color: #6666FF;">…</span> to match existing articles.
- When revising an existing article, apply<span style="color: #6666FF;">…</span> to every<h2> you add or modify — regardless of how the source article currently formats its headings. Do not preserve plain or unstyled <h2> tags from the source.
- Use short bridge paragraphs where needed (one or two sentences) before steps.
- Separate long articles with <hr> between major sections.

4) PROCEDURAL STEPS
- Primary sequences: <ol class="list-number set1"> with <li class="list-item" data-list-item-id="[stable id]">. Generate a unique opaque id per list item (alphanumeric).
- Use <em>Label</em> for UI elements users tap or click (e.g., <em>Menu</em>, <em>Admin</em>, <em>Save</em>).
- Use "Navigate to…" / "Click…" for web and portal; use "From the Menu, tap…" for mobile when the source material is mobile-first.
- Nest <ul><li data-list-item-id="…">…</li></ul> under a step for clarifications, examples, or edge cases.
- Keep one action per numbered step when possible; combine only when truly one gesture.

5) IMAGES
- Inline screenshots: <img class="wysiwyg-image-resized" …> with width/height and alt text describing the UI state—not generic words like "screenshot".
- Place images immediately after the step they illustrate.
- Preserve existing images: do not remove a pre-existing <img> from an article unless that specific image is outdated or incorrect and must be replaced. If an image does need to be replaced, remove the existing <img> tag and insert a visible placeholder element in its place:
  <div class="image-placeholder" style="background:#f5f5f5;border:2px dashed #aaa;padding:12px 16px;text-align:center;color:#888;font-style:italic;margin:8px 0;">[INSERT IMAGE HERE: description of what the replacement screenshot should show]</div>
- For new articles where the actual screenshot is not yet available, insert a visible placeholder element immediately after the relevant step in this format:
  <div class="image-placeholder" style="background:#f5f5f5;border:2px dashed #aaa;padding:12px 16px;text-align:center;color:#888;font-style:italic;margin:8px 0;">[INSERT IMAGE HERE: description of what this screenshot should show, e.g. "the Checklists screen with the 3-dot menu open"]</div>
  Use one placeholder per missing image. Do not invent image URLs or use placeholder <img> tags with fabricated src values.

6) CALLOUTS & TROUBLESHOOTING
- Use <div class="hs-callout-type-caution" data-hs-callout-type="caution"> with an <h4> inside for important constraints or safety-style notes when the article uses them.
- Troubleshooting articles: <h4> states the symptom; then h2 sections each isolate one fix path with clear verification.

7) LINKS
- External product links: include target="_blank" rel="noopener noreferrer".
- Prefer existing support article URLs when referencing permissions or related tasks; do not invent article IDs.

8) VOICE
- Clear, concise, instructional; second person (you/your) where it matches corpus examples.
- Use exact product terminology (KitchenIQ™, portal, Daypart, Setup, Checklist, To-Do) as in source.

9) QA BEFORE SUBMIT
- Heading order: intro h4, then h2 sections (do not skip levels erratically).
- Every permission mentioned appears in a warning paragraph or is clearly labeled as optional.
- Steps are testable: a reviewer can follow them literally on the stated platform.
- No placeholder lorem ipsum; use [TODO: confirm X] only if information is genuinely missing and called out in one line.
- Confirm the standard header banner image is present as the very first element in the article.
- Confirm no pre-existing images were removed unless explicitly marked for replacement with a visible placeholder element.

Produce the full article body HTML (fragment suitable for the editor), not a summary.'
  'tone_style_guidance': '- Voice: direct, supportive, operational—like onsite restaurant tech writing; avoid hype and vague superlatives.
- Sentences: short to medium; prefer bullets inside lists over long paragraphs for procedures.
- Terminology: mirror UI strings in italics; capitalize product names as in-app.
- Inclusivity: neutral, job-role oriented (leaders, team members, location managers) as in corpus.
- Consistency: same permission warning pattern and the standard Edit Role Permissions link when discussing roles.
- Formatting: preserve HubSpot-style classes (list-number set1, list-item, wysiwyg-image) for HTML compatibility.
- Honesty: if a step varies by site configuration, say what depends on what (e.g., dayparts created by the restaurant).'
  'example_content': 'Minimal pattern (abbreviated):

<figure class="wysiwyg-image">…hero…</figure>
<h4 id="…">One short paragraph: what this article does and why it matters.</h4>
<p class="warning">To edit Areas, you must have the "Manage Admin Settings" permission. … <a href="https://support.vsblapp.com/hc/en-us/articles/21775625888276" …>Edit Role Permissions</a></p>
<h2 id="…"><span style="color: #6666FF;">Navigate to Areas</span></h2>
<p>Bridge sentence.</p>
<ol class="list-number set1">
<li class="list-item" data-list-item-id="…">Click <em>Planner</em> in the left menu.</li>
<li class="list-item" data-list-item-id="…">… nested <ul><li data-list-item-id="…">clarification</li></ul></li>
</ol>

Troubleshooting pattern: h4 states problem → warning if permission → h2 sections per check (camera, network, alternate entry).'
}